node_modules/
dist/
*.log
.DS_Store
.env
.env.local
*.tsbuildinfo
# Defensive: never accept .pem files into the npm package repo.
# Public keys are embedded as TS constants; private keys live offline.
*.pem
*.key
